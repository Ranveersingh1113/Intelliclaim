# Core FastAPI dependencies
fastapi==0.111.0
uvicorn==0.29.0
python-dotenv==1.0.1
requests==2.32.3

# Document processing (pure Python, no Rust dependencies)
python-docx==1.1.0
PyPDF2==3.0.1

# AI/ML dependencies (Python 3.11 compatible)
langchain==0.2.0
langchain-community==0.2.0
langgraph==0.2.21
pydantic==2.7.1

# Google Gemini API
google-generativeai==0.5.0

# Vector database
chromadb==0.5.5

# Note: Removed PyMuPDF (Rust dependency) and other problematic packages
# PyPDF2 provides PDF processing without compilation issues
